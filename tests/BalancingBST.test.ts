import { BSTBalance } from '../src/modules/BalancingBST';

describe("BalancingBST", () => {
    it("works", () => {
        const array = [1, 3, 2, 5, 3, 6].sort();
        expect().toEqual();
    });
});